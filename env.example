# Security Configuration
# Comma-separated list of allowed domains
ALLOWED_DOMAINS=github.com,gitlab.com,bitbucket.org

# Max file size in bytes (default: 1MB)
MAX_FILE_SIZE_BYTES=1048576

# Caching Configuration
# Time to live in seconds (default: 86400 = 24h). Set to 0 to disable.
CACHE_TTL_SECONDS=86400
# Redis URL (optional). If not set, uses in-memory cache.
# REDIS_URL=redis://localhost:6379

# Max repository size in bytes (default: 100MB)
MAX_REPO_SIZE_BYTES=104857600
